<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO MAPPER 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="toy.cookingstar.repository.MemberRepository">
  <select id="getMember" resultType="toy.cookingstar.domain.Member">
    SELECT * FROM member;
  </select>
</mapper>