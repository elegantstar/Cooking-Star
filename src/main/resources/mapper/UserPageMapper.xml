<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTO MAPPER 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="toy.cookingstar.repository.UserPageRepository">

  <select id="findUserByUserId" resultType="boolean">
    SELECT EXISTS (
    SELECT *
    FROM member
    WHERE user_id = #{userId}
    );
  </select>

  <select id="findUserPageInfoByUserId" resultType="UserPageDto">
    SELECT user_id, nickname, introduction, profile_image
    FROM member
    WHERE user_id = #{userId};
  </select>

</mapper>